// En este fichero vamos a crear la interfaz user con todo lo que necesite el componente de mi app

export interface User {
  name: string;
  username: string;
  email: string;
  city: string;
  phone: string;
}
